Download Docker Desktop 
Run 'docker run -d --name oracle-db -p 1521:1521 -e ORACLE_PASSWORD=Password123 gvenzl/oracle-xe'
Run using Visual Studio Code
If you encounter a ModuleNotFoundError use the VS Code terminal 
Run 'pip install oracledb'
